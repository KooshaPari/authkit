# Clippy configuration for authvault
# Note: lint rules themselves belong in Cargo.toml [lints] or CLI `-D` flags,
# not here. clippy.toml only supports scalar config keys (see clippy docs).

msrv = "1.75"
